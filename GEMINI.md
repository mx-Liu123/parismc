Dont delete my comment when u change a line of code.
always ans in chn concisely. when u see chn, u must know i am remind u ans in chinese.
try not use fallback, when it is exception, raise error. use most neatly and radable and straightforward code
不许擅自删除测试目录和文件


代码必须在逻辑上直接简单，不要增加代码容错率，比如：
1 no use many try except, raise error
2 不要擅自加clip, regulariztion,evals = np.maximum(evals_raw, 1e-30) etc

